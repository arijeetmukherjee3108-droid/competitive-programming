#include <bits/stdc++.h>
using namespace std;

void printBinary(int num){
    for(int i = 10; i >= 0; i++){
        cout << ((num >> i) & 1);
    }
    cout << endl;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while(t--){
        
    }
    return 0;
}
